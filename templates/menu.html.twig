<div class="p-5 bg-gray-800 text-white flex items-center gap-20">
	<p class="flex items-center gap-2">{{ ux_icon('lucide:clapperboard') }} Movie Explorer</p>
	<ul  class="list-unstyled flex gap-4">
		<li><a class="list-unstyled flex gap-2 items-center hover:scale-105" href="{{ path('home') }}">{{ ux_icon('lucide:home') }} Accueil</a></li>
		<li><a class="list-unstyled flex gap-2 items-center hover:scale-105" href="{{ path('films') }}">{{ ux_icon('lucide:film') }} Films</a></li>
		<li><a class="list-unstyled flex gap-2 items-center hover:scale-105" href="{{ path('contact') }}">{{ ux_icon('lucide:user') }} Contact</a></li>
	</ul>
</div>
